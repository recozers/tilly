{
  "name": "tilly-test",
  "version": "1.0.0",
  "description": "Minimal test server for Tilly deployment",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "node test-server.js",
    "test": "echo \"Test server\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
} 